# ClinicManagementSystem

# Angular BootStrap : 

* this Empty project is angular project with all the dependencies you will need to build well - responsive application . 

* make sure that u called : `npm install` 